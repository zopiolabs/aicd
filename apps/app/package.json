{
  "name": "@aicd/app",
  "version": "0.0.1",
  "private": true,
  "description": "AICD mobile and desktop application",
  "main": "index.js",
  "scripts": {
    "dev": "echo 'Starting app development server...'",
    "build": "echo 'Building app...'",
    "test": "vitest",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist node_modules .turbo",
    "analyze": "echo 'Analyzing app bundle...'"
  },
  "dependencies": {
    "@aicd/core": "workspace:*",
    "@aicd/auth": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}